/** @type {import('next').NextConfig} */
const nextConfig = {
  i18n: {
    locales: ["en", "es"], // Lista de locales soportados
    defaultLocale: "en", // Locale por defecto
  },
};

export default nextConfig;
